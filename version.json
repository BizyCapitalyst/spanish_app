{
  "version": "1.0.2",
  "released": "2026-05-09",
  "notes": "Update flow synced with the audio-player app's pattern: same-origin ./version.json with cache-bust, controllerchange-aware applyUpdate so the post-install reload doesn't race the new service worker, network-first SW for shell, cache-first for static, version.json bypassed entirely. Chip text shortened to 'update vX.Y.Z' (no arrow) to match the audio-player chip style."
}
